# Native crash repro (not fixed): a user function referencing a module function that is
# itself a closure over a retained module value. interp: true; cranelift: NATIVE BUG
# "binding ... is not reachable".
fn f(b: Byte) -> bool:
    web::is_unreserved(b)
f(45)
